
# 🧙‍♂️ Генератор сайта «Манчкин — база знаний»

Статический генератор, создающий интерактивную базу знаний по настольной игре **Манчкин**.
Проект предназначен для систематизации **спорных и неоднозначных ситуаций** в игре, с решениями, выработанными автором на основе FAQ, официальных комментариев и личного опыта.

---

## 🎯 Цель проекта

Игроки Манчкина часто сталкиваются с противоречивыми трактовками карт и правил.
Задача этого генератора — собрать такие случаи, оформить их в едином формате и опубликовать в удобном HTML-виде с поиском, фильтрами и ссылками на источники.

Автор не просто агрегирует FAQ, а **принимает решения** по спорным ситуациям, формируя единый набор "рабочих" правил, применимых в домашней игре.

---

## 📁 Структура проекта

```
munchkin-wiki/
├── munchkin_builder.py     # Основной генератор сайта
├── munchkin_template.html  # HTML-шаблон
├── munchkin_rules.txt      # Исходный текст с правилами (FAQ)
├── style.css               # Внешние стили
├── script.js               # JS для поиска и фильтрации
├── build_site.sh           # Скрипт сборки (генерация + перенос в public/)
├── requirements.txt        # Зависимости (jinja2)
└── .github/workflows/deploy.yml  # GitHub Actions для деплоя на Pages
```

После сборки результат лежит в:

```
public/
├── index.html
├── style.css
└── script.js
```

---

## ⚙️ Установка и запуск

1. Установи зависимости:

   ```bash
   pip install -r requirements.txt
   ```

2. Сгенерируй сайт:

   ```bash
   python munchkin_builder.py
   ```

   или (рекомендуется)

   ```bash
   ./build_site.sh
   ```

3. Готовый сайт появится в папке `public/`
   Открой `public/index.html` в браузере.

---

## 🧾 Формат файла `munchkin_rules.txt`

Каждое правило отделяется `===` и описывается в формате:

```
======================================
ВОПРОС: Можно ли смываться, если в бою несколько монстров?
РЕШЕНИЕ: Да, один бросок кубика используется против всех монстров сразу, если карты не указывают иное.
ССЫЛКИ: FAQ|https://faq.example.com/escape, Форум|https://forum.example.com/multi-monsters
ТЕГИ: бой, побег, непотребства, faq
КАРТЫ: Зомби-орда, Древний дракон
======================================
```

### 🔹 Поля

| Поле        | Описание                                                        |             |       |
| ----------- | --------------------------------------------------------------- | ----------- | ----- |
| **ВОПРОС**  | Краткий заголовок вопроса                                       |             |       |
| **РЕШЕНИЕ** | Краткий ответ или разъяснение (поддерживает `\n` для переносов) |             |       |
| **ССЫЛКИ**  | Опционально. Формат: `Текст                                     | URL, Текст2 | URL2` |
| **ТЕГИ**    | Список тегов через запятую                                      |             |       |
| **КАРТЫ**   | Названия карт, к которым относится вопрос                       |             |       |

---

## 🏷️ Основные теги

| Тег                  | Что охватывает                                                       |
| -------------------- | -------------------------------------------------------------------- |
| **бой**              | подсчёт силы, помощники, бонусы, порядок боя                         |
| **непотребства**     | особенности монстров, “малыш”, “мама”, “дополнительный” и т.д.       |
| **шмотки**           | предметы, экипировка, ограничения, продажа                           |
| **особые сокровища** | мгновенные карты сокровищ, “+1 уровень”, эффекты вне боя             |
| **особые двери**     | карты дверей с особым действием (“Усиление монстра”, “Смена класса”) |
| **проклятия**        | момент применения, отмена, на кого действуют                         |
| **классы/расы**      | особенности, взаимодействие между классами и расами                  |
| **наемнички**        | наёмники и спутники, их поведение и взаимодействие                   |
| **скакуны**          | бонусы и комбинирование с другими картами                            |
| **побег**            | смывка, модификаторы, особенности броска                             |
| **уровни**           | получение и продажа уровней, ограничения “нельзя победить”           |
| **ход**              | порядок фазы хода, тайминг применения карт                           |

---

## 🧩 Шаблон сайта

HTML-шаблон использует [Jinja2](https://jinja.palletsprojects.com/).
Доступные переменные в шаблоне:

* `rules` — список всех правил
* `grouped` — правила, сгруппированные по тегам
* `categories` — список всех тегов

---

## 🧱 Автоматическая сборка и деплой

Для сборки и публикации используется GitHub Actions.
Workflow `.github/workflows/deploy.yml` автоматически:

1. Устанавливает Python и зависимости
2. Запускает `build_site.sh`
3. Копирует результат в `public/`
4. Публикует сайт на **GitHub Pages**

Запуск происходит при каждом `git push` в ветку `master`.

---

## 🚀 Быстрый деплой вручную

```bash
./build_site.sh
git add .
git commit -m "update site"
git push
```

GitHub Actions сам соберёт и обновит страницу на Pages.

---

## 💡 Совет

Храни `munchkin_rules.txt` в git — это позволит видеть историю изменений правил, когда и как корректировались спорные ситуации.

---

### ✍️ Автор

Проект поддерживается энтузиастом-модератором Манчкина,
который систематизирует разрозненные FAQ и спорные трактовки правил,
чтобы сделать игру честной, логичной и более предсказуемой.
